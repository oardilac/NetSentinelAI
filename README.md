# PySentry
A hybrid IDS, listens to the invisible data flowing through the computer's network card, organizes that data, looks for known bad behavior, and uses machine learning to spot weird, unknown behavior.
